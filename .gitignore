__pycache__/
*.py[cod]
.venv/
node_modules/
dist/
dist-*/
release/
# Self-contained backend runtime — a build artifact (scripts/build-backend-runtime.sh
# regenerates it; CI builds it fresh before packaging). ~100 MB, never committed.
runtime/
*.db
*.db.*
# Runtime logs (crawl.log, webapp_run.log, etc.)
*.log
exclude_paths.json
# Personal one-off scripts / data-rescue manifests — not part of the tool.
personal/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
